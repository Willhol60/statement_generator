<!DOCTYPE html>
<html>
  <head>
    <title>StatementGenerator</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <div style= "display: flex; justify-content: space-between">
        <h2>StatementGenerator</h2>
        <div style="color: white"><%= button_to "Sign out", destroy_user_session_path, method: :delete %></div>
      </div>
    </header>
    <%= yield %>
  </body>
</html>
